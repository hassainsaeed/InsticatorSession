{
  "name": "insticator-session",
  "private": true,
  "version": "0.0.0",
  "description": "A Javascript SDK that generates, maintains, and expires a user's session while visiting a webpage",
  "repository": "https://github.com/hassainsaeed/InsticatorSession",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^5.16.0",
    "eslint-config-airbnb-base": "^13.2.0",
    "eslint-plugin-import": "^2.18.0",
    "http-server": "^14.1.1"
  },
  "scripts": {
    "start": "http-server -a localhost -p 8000 -c-1"
  }
}
